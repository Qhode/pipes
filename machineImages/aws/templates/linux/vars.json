{
  "AWS_ACCESS_KEY": "${AWS_ACCESS_KEY}",
  "AWS_SECRET_KEY": "${AWS_SECRET_KEY}",
  "OS": "${OS}",
  "ARCHITECTURE": "${ARCHITECTURE}",
  "DOCKER_VERSION": "${DOCKER_VERSION}",
  "REGION": "${REGION}",
  "VPC_ID": "${VPC_ID}",
  "SUBNET_ID": "${SUBNET_ID}",
  "SOURCE_AMI": "${SOURCE_AMI}",
  "SECURITY_GROUP_ID": "${SECURITY_GROUP_ID}",
  "SYSTEM_RUNTIME_LANGUAGE_VERSION": "${SYSTEM_RUNTIME_LANGUAGE_VERSION}",
  "RUNTIME_VERSION": "${RUNTIME_VERSION}",
  "DOCKER_IMAGE_REGISTRY_URL": "${DOCKER_IMAGE_REGISTRY_URL}",
  "GITHUB_USERNAME": "${GITHUB_USERNAME}",
  "GITHUB_API_KEY": "${GITHUB_API_KEY}",
  "RT_URL": "${RT_URL}",
  "RT_USER": "${RT_USER}",
  "RT_API_KEY": "${RT_API_KEY}",
  "SSH_USERNAME": "${SSH_USERNAME}",
  "SSH_BASTION_HOST": "${SSH_BASTION_HOST}",
  "SSH_BASTION_USERNAME": "${SSH_BASTION_USERNAME}",
  "SSH_BASTION_PRIVATE_KEY_PATH": "${SSH_BASTION_PRIVATE_KEY_PATH}"
}
